# AppSettings + Dependency Injection Example
Example repository which shows the setup of the HostingEnvironment, AppSettings.json loading, Dependency Injection and NLog in a .NET 6-Console Application.
